---
title: Sort an Object
tags:
    - Javascript
logo: Javascript
isPublished: true
publishedDate: 2023-05-13
---

```js
const sortObject = (obj) =>
	Object.keys(obj)
		.sort()
		.reduce(
			(acc, curr) => ({
				...acc,
				[curr]: obj[curr],
			}),
			{}
		);
```
